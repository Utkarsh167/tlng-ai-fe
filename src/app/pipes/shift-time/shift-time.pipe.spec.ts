import { ShiftTimePipe } from './shift-time.pipe';

describe('ShiftTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftTimePipe();
    expect(pipe).toBeTruthy();
  });
});

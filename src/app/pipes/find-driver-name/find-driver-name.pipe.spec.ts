import { FindDriverNamePipe } from './find-driver-name.pipe';

describe('FindDriverNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FindDriverNamePipe();
    expect(pipe).toBeTruthy();
  });
});

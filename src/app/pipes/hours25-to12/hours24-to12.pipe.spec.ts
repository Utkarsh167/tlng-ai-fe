import { Hours24To12Pipe } from './hours24-to12.pipe';

describe('Hours24To12Pipe', () => {
  it('create an instance', () => {
    const pipe = new Hours24To12Pipe();
    expect(pipe).toBeTruthy();
  });
});
